
  # Game Up Student animated

  This is a code bundle for Game Up Student animated. The original project is available at https://www.figma.com/design/H0I40O0kyEM69TiE4irAHt/Game-Up-Student-animated.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  